# Project 2

## Release the Data

Go to:

```
/workspace/data/
```

Unzip `x_test_final_fIrnA7Q.csv.zip` and `x_train_final_asAbTs5.csv.zip`, then you will get `x_test_final_fIrnA7Q.csv` and `x_train_final_asAbTs5.csv`.

You will get the file `XAndY_Preprocess.csv` after running the part 1 of the code.

## Setup the Virtual Environment

* Here I use Python 3.14.4 for the demonstration.

```
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```
